<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".Data"
            android:label="Monthly/daily Data"
            android:parentActivityName=".Main"
            ></activity>
        <activity android:name=".SystemActivation"
            android:label="System Activation"
            android:parentActivityName=".Main"
            />
        <activity android:name=".Notifications"
            android:label="Notifications"
            android:parentActivityName=".Main"
            />
        <activity
            android:name=".Automation"
            android:label="Automation"
            android:parentActivityName=".Main" />
        <activity
            android:name=".Valves"
            android:label="Valves"
            android:parentActivityName=".Main" />
        <activity
            android:name=".Main"
            android:label="Greenhouse">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>